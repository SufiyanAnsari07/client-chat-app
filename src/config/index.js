export const config = {
    serverUrl:String(import.meta.env.VITE_SERVER_URL),
    meteredUsername:String(import.meta.env.VITE_METERED_USERNAME),
    meteredPassword:String(import.meta.env.VITE_METERED_PASSWORD),
}